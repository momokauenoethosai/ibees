# アプリケーションモード設定
# デモモード（認証不要）: true
# 本番モード（認証必要）: false
DEMO_MODE=false

# Google Cloud認証ファイルのパス（本番モードで必要）
# 1. gcloud auth application-default login を実行
# 2. ~/.config/gcloud/application_default_credentials.json をプロジェクト内にコピー
# 3. 下記のパスを設定
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json